# Flick codebase memory

<!-- Generated by scripts/update-memory.mjs. Do not edit. -->
<!-- fingerprint:ac57fc8a33bcfe488c5a8e91ec8c2575872d1acbb6d47ebfbc454cdbe5da2a0d -->

This file is the long-term memory of the git codebase. It is regenerated
from the working tree whenever `npm run memory:update` or the git hooks
run, so agents and humans can trust it over stale chat history.

## Intent

# Flick — durable product facts

Edit this file when product rules change. `scripts/update-memory.mjs`
copies it into the generated snapshot.

## What it is

A browser flashcard app. The user adds a word, the English translation,
and an optional example, then reviews unread cards.

There is no account. On localhost, the deck is `data/cards.json` via
`/api/cards` so agents can upload words. On a public host (Vercel), the
phone browser keeps the deck in `localStorage` only.

## Learning rules

- Each day, Start learning only deals cards whose `dueAt` is today or
  earlier.
- Each card randomly starts from the word or the English. The learner
  flips it to see the other side and the example.
- **Yes** (`reviewCard(card, true)`): interval goes 1 day, then 3, then
  7, then `round(interval * ease)`. The card is due on that later day.
- **No** (`reviewCard(card, false)`): interval resets to 1 day, ease
  drops a little, and the card is due tomorrow — not again this session.
- Tap flips the card. Swipe right is Yes, swipe left is No. Space /
  Enter flips. `Y` / `N` also answer. Desktop still has Yes/No buttons.
- The app is a PWA (`app/manifest.ts`) so Android Chrome can Add to
  Home screen and run it fullscreen. Public deploys are Vercel.
- New cards are due today. `--known` on upload schedules the first
  review for tomorrow.

## Stack

Next.js App Router, React, TypeScript, Tailwind, shadcn/ui. Dev server
port `43147`.

## Surfaces

- `/` — deck stats, add/delete cards, start learning
- `/learn` — one-pass Yes/No session
- `POST /api/cards` — agent or CLI upload of a word
- `GET /api/cards?learned=1` — words the local user already knows

## Agent upload

When a word is learned with the local user, add it with
`npm run add-word -- --word <word> --translation <english> --example <sentence> --known`.
The same word is upserted. `--known` marks it already learned.

## Snapshot

- **Source fingerprint:** `ac57fc8a33bcfe48`
- **Tracked source files:** 32
- **Routes:** `/`, `/learn`, `API /api/cards`

## Layout

- `./`
  - `README.md`
  - `next.config.ts`
  - `package.json`
- `app/`
  - `apple-icon.tsx`
  - `globals.css`
  - `icon.tsx`
  - `layout.tsx`
  - `manifest.ts`
  - `page.tsx`
- `app/api/cards/`
  - `route.ts`
- `app/learn/`
  - `page.tsx`
- `components/`
  - `add-card-dialog.tsx`
  - `android-install-hint.tsx`
  - `card-list.tsx`
  - `flashcard.tsx`
  - `home-view.tsx`
  - `pwa-register.tsx`
  - `site-header.tsx`
- `hooks/`
  - `use-card-swipe.ts`
- `lib/`
  - `api.ts`
  - `card-model.ts`
  - `cards-file.ts`
  - `deck-context.tsx`
  - `deck-store.ts`
  - `schedule.ts`
  - `storage.ts`
  - `types.ts`
  - `use-has-mounted.ts`
  - `utils.ts`
- `scripts/`
  - `add-word.mjs`
  - `install-hooks.mjs`
  - `update-memory.mjs`

## Library exports

- `lib/api.ts`: `API_HEADERS`, `json`
- `lib/card-model.ts`: `AddCardInput`, `SAMPLE_CARDS`, `createCard`, `isFlashcard`, `normalizeWord`, `parseAddCardInput`, `parseCards`, `upsertCard`
- `lib/cards-file.ts`: `CARDS_FILE`, `addCardToDeck`, `readDeck`, `resetDeckSchedule`, `reviewCardInDeck`, `writeDeck`
- `lib/deck-context.tsx`: `DeckProvider`, `useDeck`
- `lib/deck-store.ts`: `addCard`, `deleteCard`, `getClientSnapshot`, `getServerSnapshot`, `markKnown`, `resetKnown`, `review`, `subscribe`
- `lib/schedule.ts`: `DEFAULT_EASE`, `addDays`, `daysUntilDue`, `dueCards`, `endOfDay`, `formatDue`, `formatNextReview`, `isDue`, `nextDueAt`, `normalizeCard`, `resetSchedule`, `reviewCard`, `scheduleNewCard`, `startOfDay`, `upcomingCards`
- `lib/storage.ts`: `MIGRATED_KEY`, `SAMPLE_CARDS`, `STORAGE_KEY`, `canUseLocalApi`, `hasMigratedLocalDeck`, `loadCards`, `markLocalDeckMigrated`, `saveCards`
- `lib/types.ts`: `Flashcard`
- `lib/use-has-mounted.ts`: `useHasMounted`
- `lib/utils.ts`: `cn`

## File index

| File | Lines | Exports |
| --- | ---: | --- |
| `README.md` | 78 | — |
| `app/api/cards/route.ts` | 108 | DELETE, GET, OPTIONS, PATCH, POST, dynamic, runtime |
| `app/apple-icon.tsx` | 29 | contentType, default, size |
| `app/globals.css` | 136 | — |
| `app/icon.tsx` | 29 | contentType, default, size |
| `app/layout.tsx` | 60 | default, metadata, viewport |
| `app/learn/page.tsx` | 249 | default |
| `app/manifest.ts` | 31 | default |
| `app/page.tsx` | 14 | default |
| `components/add-card-dialog.tsx` | 148 | AddCardDialog |
| `components/android-install-hint.tsx` | 29 | AndroidInstallHint |
| `components/card-list.tsx` | 130 | CardList |
| `components/flashcard.tsx` | 153 | Flashcard, PromptSide, SwipeHints |
| `components/home-view.tsx` | 114 | HomeView |
| `components/pwa-register.tsx` | 15 | PwaRegister |
| `components/site-header.tsx` | 18 | SiteHeader |
| `hooks/use-card-swipe.ts` | 106 | useCardSwipe |
| `lib/api.ts` | 10 | API_HEADERS, json |
| `lib/card-model.ts` | 152 | AddCardInput, SAMPLE_CARDS, createCard, isFlashcard, normalizeWord, parseAddCardInput, parseCards, upsertCard |
| `lib/cards-file.ts` | 74 | CARDS_FILE, addCardToDeck, readDeck, resetDeckSchedule, reviewCardInDeck, writeDeck |
| `lib/deck-context.tsx` | 89 | DeckProvider, useDeck |
| `lib/deck-store.ts` | 258 | addCard, deleteCard, getClientSnapshot, getServerSnapshot, markKnown, resetKnown, review, subscribe |
| `lib/schedule.ts` | 200 | DEFAULT_EASE, addDays, daysUntilDue, dueCards, endOfDay, formatDue, formatNextReview, isDue, nextDueAt, normalizeCard, resetSchedule, reviewCard, scheduleNewCard, startOfDay, upcomingCards |
| `lib/storage.ts` | 54 | MIGRATED_KEY, SAMPLE_CARDS, STORAGE_KEY, canUseLocalApi, hasMigratedLocalDeck, loadCards, markLocalDeckMigrated, saveCards |
| `lib/types.ts` | 13 | Flashcard |
| `lib/use-has-mounted.ts` | 12 | useHasMounted |
| `lib/utils.ts` | 2 | cn |
| `next.config.ts` | 8 | default |
| `package.json` | 37 | — |
| `scripts/add-word.mjs` | 184 | — |
| `scripts/install-hooks.mjs` | 22 | — |
| `scripts/update-memory.mjs` | 245 | — |
