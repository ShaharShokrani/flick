# Flick codebase memory

<!-- Generated by scripts/update-memory.mjs. Do not edit. -->
<!-- fingerprint:77a5b71e06ff28110193de20d6c5f9688e9ab1a30043f30600a5bc645a125109 -->

This file is the long-term memory of the git codebase. It is regenerated
from the working tree whenever `npm run memory:update` or the git hooks
run, so agents and humans can trust it over stale chat history.

## Intent

# Flick — durable product facts

Edit this file when product rules change. `scripts/update-memory.mjs`
copies it into the generated snapshot.

## What it is

A browser flashcard app. The user adds a word, the English translation,
and an optional example, then reviews unread cards.

There is no account. The local user's deck is `data/cards.json`, exposed
at `/api/cards`. The browser keeps a `localStorage` cache and polls the
API so agent uploads show up without a refresh.

## Learning rules

- A session starts with every card that is not `known`, shuffled once.
- The card shows the word first. The learner flips it to see English and
  the example.
- **Yes** calls `markKnown`. That card stays out of later sessions until
  `resetKnown`.
- **No** removes the card from the current queue only. It comes back the
  next time the user starts learning.
- Space / Enter flips. `Y` / `N` answer after a flip.

## Stack

Next.js App Router, React, TypeScript, Tailwind, shadcn/ui. Dev server
port `43147`.

## Surfaces

- `/` — deck stats, add/delete cards, start learning
- `/learn` — one-pass Yes/No session
- `POST /api/cards` — agent or CLI upload of a word
- `GET /api/cards?learned=1` — words the local user already knows

## Agent upload

When a word is learned with the local user, add it with
`npm run add-word -- --word <word> --translation <english> --example <sentence> --known`.
The same word is upserted. `--known` marks it already learned.

## Snapshot

- **Source fingerprint:** `77a5b71e06ff2811`
- **Tracked source files:** 25
- **Routes:** `/`, `/learn`, `API /api/cards`

## Layout

- `./`
  - `README.md`
  - `next.config.ts`
  - `package.json`
- `app/`
  - `globals.css`
  - `layout.tsx`
  - `page.tsx`
- `app/api/cards/`
  - `route.ts`
- `app/learn/`
  - `page.tsx`
- `components/`
  - `add-card-dialog.tsx`
  - `card-list.tsx`
  - `flashcard.tsx`
  - `home-view.tsx`
  - `site-header.tsx`
- `lib/`
  - `api.ts`
  - `card-model.ts`
  - `cards-file.ts`
  - `deck-context.tsx`
  - `deck-store.ts`
  - `storage.ts`
  - `types.ts`
  - `use-has-mounted.ts`
  - `utils.ts`
- `scripts/`
  - `add-word.mjs`
  - `install-hooks.mjs`
  - `update-memory.mjs`

## Library exports

- `lib/api.ts`: `API_HEADERS`, `json`
- `lib/card-model.ts`: `AddCardInput`, `SAMPLE_CARDS`, `createCard`, `isFlashcard`, `normalizeWord`, `parseAddCardInput`, `parseCards`, `upsertCard`
- `lib/cards-file.ts`: `CARDS_FILE`, `addCardToDeck`, `readDeck`, `writeDeck`
- `lib/deck-context.tsx`: `DeckProvider`, `useDeck`
- `lib/deck-store.ts`: `addCard`, `deleteCard`, `getClientSnapshot`, `getServerSnapshot`, `markKnown`, `resetKnown`, `subscribe`
- `lib/storage.ts`: `MIGRATED_KEY`, `SAMPLE_CARDS`, `STORAGE_KEY`, `hasMigratedLocalDeck`, `loadCards`, `markLocalDeckMigrated`, `saveCards`
- `lib/types.ts`: `Flashcard`
- `lib/use-has-mounted.ts`: `useHasMounted`
- `lib/utils.ts`: `cn`

## File index

| File | Lines | Exports |
| --- | ---: | --- |
| `README.md` | 68 | — |
| `app/api/cards/route.ts` | 99 | DELETE, GET, OPTIONS, PATCH, POST, dynamic, runtime |
| `app/globals.css` | 135 | — |
| `app/layout.tsx` | 42 | default, metadata |
| `app/learn/page.tsx` | 242 | default |
| `app/page.tsx` | 14 | default |
| `components/add-card-dialog.tsx` | 148 | AddCardDialog |
| `components/card-list.tsx` | 118 | CardList |
| `components/flashcard.tsx` | 71 | Flashcard |
| `components/home-view.tsx` | 102 | HomeView |
| `components/site-header.tsx` | 18 | SiteHeader |
| `lib/api.ts` | 10 | API_HEADERS, json |
| `lib/card-model.ts` | 138 | AddCardInput, SAMPLE_CARDS, createCard, isFlashcard, normalizeWord, parseAddCardInput, parseCards, upsertCard |
| `lib/cards-file.ts` | 49 | CARDS_FILE, addCardToDeck, readDeck, writeDeck |
| `lib/deck-context.tsx` | 79 | DeckProvider, useDeck |
| `lib/deck-store.ts` | 225 | addCard, deleteCard, getClientSnapshot, getServerSnapshot, markKnown, resetKnown, subscribe |
| `lib/storage.ts` | 45 | MIGRATED_KEY, SAMPLE_CARDS, STORAGE_KEY, hasMigratedLocalDeck, loadCards, markLocalDeckMigrated, saveCards |
| `lib/types.ts` | 9 | Flashcard |
| `lib/use-has-mounted.ts` | 12 | useHasMounted |
| `lib/utils.ts` | 2 | cn |
| `next.config.ts` | 8 | default |
| `package.json` | 37 | — |
| `scripts/add-word.mjs` | 164 | — |
| `scripts/install-hooks.mjs` | 22 | — |
| `scripts/update-memory.mjs` | 245 | — |
