#!/usr/bin/env sh
set -e

# Git passes previous HEAD, new HEAD, and a flag: 1 means branch checkout.
if [ "$3" != "1" ] || [ "${SKIP_MEMORY}" = "1" ]; then
  exit 0
fi

node scripts/update-memory.mjs
